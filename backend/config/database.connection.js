// mongodb schemea to basically hold the data that we insert and also take in the AI response as well.  
const mongoose = require('mongoose');
const URI = process.env.URI; 

const clusterConnection = async () => {
    try { 
        
    } catch (error) { 

    }
}